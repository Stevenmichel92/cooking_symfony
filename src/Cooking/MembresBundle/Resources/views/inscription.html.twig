{% extends 'base.html.twig' %}

{% block body %}
<nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="index">Home</a></li>
                <li class="breadcrumb-item"><a href="connexion">Connexion</a></li>
                <li class="breadcrumb-item active" aria-current="page">Inscription</li>
            </ol>
        </nav>   

        <div class="container recettes_body">
            <div class="row justify-content-center">
                <div class ="col-sm-12 col-md-6">
                    <br>
                    <h3>Je m'inscris</h3>

                     {{ form_start(form_inscription) }}
                        <div class="form-group">
                            Prénom
                            {{ form_widget(form_inscription.prenom, { 'attr': {'class': 'form-control', 'size': '30'} }) }}
                        </div>

                        <div class="form-group">
                            Nom
                            {{ form_widget(form_inscription.nom, { 'attr': {'class': 'form-control', 'size': '30'} }) }}
                        </div>

                        <div class="form-group">
                            Login
                            {{ form_widget(form_inscription.login, { 'attr': {'class': 'form-control', 'size': '30'} }) }}
                        </div>

                        <div class="form-group">
                            Mot de passe
                            {{ form_widget(form_inscription.password, { 'attr': {'class': 'form-control', 'size': '30'} }) }}
                        </div>

                        <div class="row justify-content-center">
                                {{ form_widget(form_inscription.save, { 'attr': {'class': 'btn btn-info'} }) }}
                        </div>
                </div>   
                    {{ form_end(form_inscription) }}
                </div>   
            </div>   
        </div>    

{% endblock %}