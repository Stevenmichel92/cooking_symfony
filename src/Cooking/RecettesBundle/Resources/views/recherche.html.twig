{% extends 'base.html.twig' %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="index">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">Recherche : {{ app.session.get('recherche') }}</li>
        </ol>
    </nav>   

    <div class="container recettes_body">
        <br><h4>Voici les résultats de votre recherche : <br><strong>{{ app.session.get('recherche') }}</strong></h4>
          <div class="row recette_index_forme"> 

                {% for recette in recettes %}
                    <div class="col-sm-6 col-md-6 col-lg-4" style="margin-top:2%">
                        {% include '@CookingRecettes/Default/recettes_card.html.twig' %}
                    </div>
                {% endfor %}
        
        </div>
    </div>    
{% endblock %}